# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
*.egg-info/

# Secrets — NUNCA commitar
.env
.env.*
data/user_credentials.json

# Ficheiros de runtime gerados pelo serve.py / bot
data/beta/user_universe.json
news.json
data/wipe_archive/

# Claude Code settings — local only
.claude/

# Runtime locks — gerados em cada ciclo, não são código
bonnie.lock
bot.lock

# Dados pesados — backtests e modelos ML locais
data/backtest/
data/models/

# Calibração offline — cache de dados e resultados de sweep (gerados localmente)
data/cache/
data/backtest_cache/
data/calibration/

# Logs de runtime diários — efémeros, não são código
logs/trades/
logs/errors/

# Artefactos de backtest e learner commitados por engano (não são código)
data/*.log
data/*.csv
data/learner_corpus/

# Artifacts de desenvolvimento local
backtest_run.log
prompt-*.txt
future-*.txt

# Ferramentas externas — não são código do projecto
.obsidian/
graphify-out/
